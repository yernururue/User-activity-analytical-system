from user import User
from activity import Activity

class AnalyticsSystem:
    def __init__(self, users, activities):
        self.users = {user_id: User}
        self.activities = activities.append(action)
    

